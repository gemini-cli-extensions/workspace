{
  "name": "google-workspace",
  "version": "DEV",
  "contextFileName": "workspace-mcp-server${/}WORKSPACE-Context.md",
  "mcpServers": {
    "google-workspace": {
      "command": "node",
      "args": ["scripts${/}start.js"],
      "cwd": "${extensionPath}"
    }
  }
}
