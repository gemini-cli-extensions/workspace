/**
 * @fileoverview Google OAuth login + callback handler — placeholder.
 *
 * TODO(task 6): replace with the real Google OAuth login + callback flow.
 */

export async function handleGoogleAuth(_request: Request, _env: Env): Promise<Response> {
  return new Response("auth not yet wired", { status: 501 });
}
