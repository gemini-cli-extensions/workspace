---
/**
 * Root landing (`/`). This app's real landing page is `/gws` (the Google
 * Workspace MCP tool overview); the old ecosystem-showcase template landing
 * moved to `/template` verbatim so it stays reachable.
 */
return Astro.redirect("/gws");
---
